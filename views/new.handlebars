<div class="expense-container mt-4 px-5">
  <div class="expense-link">
    <a href="/" class="lastpage-btn mt-4">
      <i class="fa-solid fa-angle-left"></i>
    </a>
    <h2 class="pb-4 mt-4">請輸入支出</h2>
    <a href="/users/logout" class="logout-btn mt-4">
      <i class="fa-solid fa-right-from-bracket"></i>
    </a>
  </div>
  {{!-- expense form --}}
  <div class="form-container px-5 mt-4">
    <form id="form" action="/expenses" method="POST" novalidate>
      <div class="form-item">
        <i class="fa-solid fa-cart-shopping"></i>
        <input id="name" type="text" name="name" placeholder="請輸入名稱" required>
      </div>
      <div class="form-item">
        <i class="fa-solid fa-calendar-days"></i>
        <input id="date" type="date" name="date" required>
      </div>
      <div class="form-item">
        <select id="category" name="category" class="form-select form-select-lg mb-3"
          aria-label=".form-select-lg example" required>
          <option selected>類別</option>
          <option value="家居物業" {{#if life}} selected {{/if}}>家居物業</option>
          <option value="交通出行" {{#if traffic}} selected {{/if}}>交通出行</option>
          <option value="休閒娛樂" {{#if recreation}} selected {{/if}}>休閒娛樂</option>
          <option value="餐飲食品" {{#if food}} selected {{/if}}>餐飲食品</option>
          <option value="其他" {{#if other}} selected {{/if}}>其他</option>
        </select>
      </div>
      <div class="form-item">
        <i class="fa-solid fa-dollar-sign"></i>
        <input id="amount" type="number" name="amount" placeholder="請輸入金額" required>
      </div>
      <div class="form-item text-center">
        <button id="submit-button" class="btn btn-primary" type="submit" style="background-color: #EFA3C8;">新增</button>
      </div>
    </form>
  </div>
</div>